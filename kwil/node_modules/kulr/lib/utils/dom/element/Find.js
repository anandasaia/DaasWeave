"use strict";

Object.defineProperty(exports, "__esModule", { value: true });
/**
 * find the first matching element
 * @param selector
 */
function findOne(selector) {
    //  if we've been given an HTML element, return it;
    if (isSelectorElement(selector)) return selector;
    //  find the element(s) with the matching selector
    return document.querySelector(selector);
}
exports.findOne = findOne;
/**
 * check if a selector is an HTML element
 * @param selector
 */
function isSelectorElement(selector) {
    try {
        var window = void 0;
        //  get the window owner for this element
        window = selector.ownerDocument.defaultView;
        //  test the object's type
        return selector instanceof window.HTMLElement;
    } catch (e) {
        return false;
    }
}
/**
 * find an element with a given data-role within an element
 * @param selector
 * @param role
 * @return {any}
 */
function findByRoleWithin(selector, role) {
    try {
        return findOne(selector).querySelector('[data-role="' + role + '"]');
    } catch (e) {
        console.warn('no element found');
        return null;
    }
}
exports.findByRoleWithin = findByRoleWithin;
/**
 * find multiple elements with a given data-role within an element
 * @param selector
 * @param role
 * @return {any}
 */
function findByRolesWithin(selector, role) {
    try {
        return findOne(selector).querySelectorAll('[data-role="' + role + '"]');
    } catch (e) {
        return null;
    }
}
exports.findByRolesWithin = findByRolesWithin;