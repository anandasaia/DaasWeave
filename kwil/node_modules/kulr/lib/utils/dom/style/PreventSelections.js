"use strict";

Object.defineProperty(exports, "__esModule", { value: true });
/**
 * prevent selections in the document
 * @param allowSelections
 */
function allowSelections() {
    var allowSelections = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;

    var styles = void 0,
        value = void 0;
    //  create a list of all the non-editable styles
    styles = ['-webkit-touch-callout', '-webkit-user-select', '-khtml-user-select', '-moz-user-select', '-ms-user-select', 'user-select'];
    //  get the value we want to set
    value = allowSelections === true ? null : 'none';
    //  add or remove all styles
    styles.map(function (style) {
        return document.body.style.setProperty(style, value);
    });
}
exports.allowSelections = allowSelections;