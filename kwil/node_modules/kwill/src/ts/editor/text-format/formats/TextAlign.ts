import * as log from 'loglevel';
import {styleGroup} from "./SetStyle";


/**
 * set text align
 * @param textAlign
 */
export function setTextAlign(textAlign: string)
{
    styleGroup('text-align', textAlign);
}