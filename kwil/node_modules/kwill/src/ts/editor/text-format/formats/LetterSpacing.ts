import * as log from 'loglevel';
import {styleGroup} from "./SetStyle";
import {KWILL_CONSTS} from "../../../constants/Constants";
import {hasUnits} from "../utils/UnitsUtils";


/**
 * set line height for the selected text
 * @param letterSpacing
 */
export function setLetterSpacing(letterSpacing: string)
{
    styleGroup(KWILL_CONSTS.textFormats.LETTER_SPACING, parseLetterSpacing(letterSpacing));
}


/**
 * ensure the letter-spacing string is in a usable format
 * @param letterSpacing
 */
function parseLetterSpacing(letterSpacing: string) : string
{
    //  does the given string already include units? if so, let's use it

    if (hasUnits(letterSpacing)) return letterSpacing;

    //  if not, let's default to px

    return parseFloat(letterSpacing) + 'px';
}