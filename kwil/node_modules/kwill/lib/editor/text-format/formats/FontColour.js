"use strict";

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

Object.defineProperty(exports, "__esModule", { value: true });
function setTextColour(fontColour) {
    document.execCommand('foreColor', false, fontColour);
}
exports.setTextColour = setTextColour;
function setBackgroundColour(backgroundColour) {
    document.execCommand('backColor', false, backgroundColour);
}
exports.setBackgroundColour = setBackgroundColour;
function extractFontColour(input) {
    switch (typeof input === "undefined" ? "undefined" : _typeof(input)) {
        case 'string':
            return input;
        case 'object':
            return input.target.value;
        default:
            return 'black';
    }
}
exports.extractFontColour = extractFontColour;