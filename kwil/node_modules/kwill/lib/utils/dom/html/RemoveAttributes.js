"use strict";

Object.defineProperty(exports, "__esModule", { value: true });
function removeAttributes(nodes, attribute) {
    var result = void 0;
    result = nodes.filter(function (node) {
        return hasAttribute(node, attribute);
    });
    result.map(function (node) {
        return node.removeAttribute(attribute);
    });
    return result;
}
exports.removeAttributes = removeAttributes;
function hasAttribute(element, attribute) {
    return !!(element.getAttribute && element.getAttribute(attribute));
}