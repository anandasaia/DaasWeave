/// <reference types="node" />
import { AxiosResponse, AxiosRequestConfig, AxiosInstance } from "axios";
import { Config } from "./config";
export declare abstract class Api {
    private readonly host;
    config: Config;
    protected constructor(host: string, opts: Config);
    private mergeDefaults;
    protected get<T = any>(endpoint: string, config?: AxiosRequestConfig): Promise<AxiosResponse<T>>;
    protected post<T = any>(endpoint: string, body: Buffer | string | object, config?: AxiosRequestConfig): Promise<AxiosResponse<T>>;
    /**
     * Get an AxiosInstance with the base configuration setup to fire off
     * a request to the network.
     */
    protected request(): AxiosInstance;
}
