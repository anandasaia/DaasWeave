import { BigNumberish, ContractTransactionResponse } from "ethers";
import { ContractInterface, Signer, ethers } from "ethers5";
export declare function v5Override(provider: Signer | ethers.Wallet, contract: ethers.Contract, method: string, args: any[]): Promise<object>;
export declare class EscrowV5 {
    private readonly contract;
    private readonly signer;
    private tokenAddress?;
    private readonly validatorAddress;
    constructor(validatorAddress: string, poolAddress: string, abi: ContractInterface, signer: Signer | ethers.Wallet);
    getTokenAddress(): Promise<string>;
    getDepositedBalance(address: String): Promise<BigNumberish>;
    private createOverride;
    deposit(amount: BigNumberish, override?: object): Promise<ContractTransactionResponse>;
}
