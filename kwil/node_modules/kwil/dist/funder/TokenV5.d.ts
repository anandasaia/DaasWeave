import { Signer, ethers } from "ethers5";
import { BigNumberish, ContractTransactionResponse } from "ethers";
export declare class TokenV5 {
    private readonly contract;
    private readonly signer;
    private name?;
    private symbol?;
    private decimals?;
    private totalSupply?;
    constructor(tokenAddress: string, abi: ethers.ContractInterface, signer: Signer | ethers.Wallet);
    getName(): Promise<string>;
    getSymbol(): Promise<string>;
    getDecimals(): Promise<number>;
    getTotalSupply(): Promise<number>;
    getBalance(address: string): Promise<BigNumberish>;
    getAllowance(owner: string, spender: string): Promise<BigNumberish>;
    private createOverride;
    approve(spender: string, amount: BigNumberish, override?: object): Promise<ContractTransactionResponse>;
}
